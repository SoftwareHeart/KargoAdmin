@using KargoAdmin.Services
@inject ILanguageService LanguageService
@inject ISettingsService Settings
@{
    ViewData["Title"] = LanguageService.GetLocalizedText("İletişim", "Contact");
    Layout = "~/Views/Shared/_PublicLayout.cshtml";
}

<link href="~/css/contact.css" rel="stylesheet" />

<div class="contact-container">
    <div class="contact-header">
        <p class="contact-breadcrumb">@LanguageService.GetLocalizedText("İletişim Bilgileri", "Contact Information")</p>
        <h1 class="contact-title">@LanguageService.GetLocalizedText("Size yardımcı olmaktan her zaman mutluluk duyarız", "We are always happy to help you")</h1>
    </div>

    <div class="contact-info-section">
        <div class="contact-cards">
            <!-- E-posta Kartı -->
            <div class="contact-card">
                <div class="contact-icon email-icon">
                    <img src="~/img/mail-icon.png" alt="E-posta İkonu" class="contact-icon-img">
                </div>
                <h3 class="contact-card-title">@LanguageService.GetLocalizedText("E-posta", "Email")</h3>
                <p class="contact-card-description"></p>
                <a href="mailto:@(Settings.GetValue("ContactEmail") ?? "help@info.com")" class="contact-link">@(Settings.GetValue("ContactEmail") ?? "help@info.com")</a>
            </div>

            <!-- Telefon Kartı -->
            <div class="contact-card">
                <div class="contact-icon phone-icon">
                    <img src="~/img/phone-icon.png" alt="Telefon İkonu" class="contact-icon-img">
                </div>
                <h3 class="contact-card-title">@LanguageService.GetLocalizedText("Telefon", "Phone")</h3>
                <p class="contact-card-description"></p>
                <a href="tel:@(Settings.GetValue("ContactPhone") ?? "+7-843-672-431")" class="contact-link">@(Settings.GetValue("ContactPhone") ?? "+7-843-672-431")</a>
            </div>

            <!-- Adres Kartı -->
            <div class="contact-card">
                <div class="contact-icon location-icon">
                    <img src="~/img/address-icon.png" alt="Adres İkonu" class="contact-icon-img">
                </div>
                <h3 class="contact-card-title">@LanguageService.GetLocalizedText("Adres", "Address")</h3>
                <p class="contact-card-description"></p>
                <div class="contact-address">
                    <span class="contact-link">@(Settings.GetValue("Address") ?? LanguageService.GetLocalizedText("Sokak / Mahalle / İlçe / İl Açık", "Street / Neighbourhood / District / City"))</span>
                </div>
            </div>
        </div>
    </div>

    <!-- Sosyal Medya İkonları -->
    <div class="social-media-section">
        <div class="social-icons">
            <a href="@(Settings.GetValue("LinkedInUrl") ?? "#")" class="social-icon linkedin-icon" target="_blank" rel="noopener">
                <i class="fab fa-linkedin-in"></i>
            </a>
            <a href="@(Settings.GetValue("InstagramUrl") ?? "#")" class="social-icon instagram-icon" target="_blank" rel="noopener">
                <i class="fab fa-instagram"></i>
            </a>
            <a href="@(Settings.GetValue("TwitterUrl") ?? "#")" class="social-icon twitter-icon" target="_blank" rel="noopener">
                <i class="fab fa-twitter"></i>
            </a>
        </div>
    </div>
</div>